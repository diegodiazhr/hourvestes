rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Rules for development: Allow read/write for any authenticated user
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
